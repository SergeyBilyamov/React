# Brand Shop – Интернет-магазин одежды

Данный проект представляет собой фронтенд-часть интернет-магазина, разработанную на React. Реализованы каталог товаров, корзина, фильтрация, адаптивная вёрстка и другие функции.

## 🚀 Функциональность

### Страницы и маршруты (React Router)
- **Главная (`/`)** – приветственный экран, акционные предложения, список избранных товаров, преимущества, форма подписки.
- **Каталог (`/catalog`)** – отображение всех товаров с возможностью фильтрации по размеру (XS, S, M, L, XL) и цвету (Red, Blue, Green, Black, White, Yellow).
- **Страница товара (`/product/:id`)** – детальная информация о товаре, кнопка добавления в корзину, блок похожих товаров (3 на десктопе, 2 на планшете).
- **Корзина (`/cart`)** – список добавленных товаров, изменение количества, удаление, расчёт общей суммы (GRAND TOTAL), очистка корзины.
- **Регистрация (`/registration`)** – форма с полями имени, пола, email и пароля, а также список преимуществ лояльности.

### Корзина (Context API)
- Управление состоянием корзины через `CartContext`.
- Добавление товара, изменение количества, удаление, полная очистка.
- Данные сохраняются в `localStorage`, поэтому корзина не теряется при перезагрузке.
- Автоматический пересчёт общей суммы.

### Фильтрация в каталоге
- Фильтр по размеру (чекбоксы) – показываются только товары, у которых в массиве `sizes` есть выбранный размер.
- Фильтр по цвету – аналогично для поля `colors`.
- Фильтры работают совместно (AND-логика: товар должен соответствовать и выбранным размерам, и выбранным цветам).
- Выпадающие меню фильтров закрываются при клике вне области (как бургер-меню).

### Адаптивная вёрстка (SCSS)
- Три основных брейкпоинта: десктоп (≥1025px), планшет (768–1024px), мобильные (≤767px).
- На планшетах товары в каталоге располагаются в две колонки.
- На мобильных – одна колонка, бургер-меню адаптируется.
- Специальные стили для планшетов на странице продукта (2 похожих товара вместо 3).

### Боковое меню (бургер)
- Открывается по клику на иконку, закрывается по клику на оверлей или повторному клику на иконку.
- Содержит пункты MAN, WOMAN, KIDS с подменю.

### Дополнительные компоненты
- `ProductCard` – карточка товара с изображением, названием, описанием, ценой и кнопкой добавления в корзину. При наведении появляется кнопка, при клике на изображение или название – переход на страницу товара.
- `FilterSort` – блок фильтрации и сортировки с выпадающими меню.
- `PageHeader` – повторяющийся заголовок страниц (используется на страницах каталога, корзины, регистрации, продукта).
- `Hero`, `Offer`, `Feature`, `Subscribe` – секции главной страницы.

## 🛠 Технологии
- **React** (функциональные компоненты, хуки)
- **React Router v6** (маршрутизация)
- **Context API** (управление корзиной)
- **SCSS** (стилизация, переменные, медиазапросы)
- **Create React App** (сборка проекта)

## 📦 Установка и запуск

1. Клонируйте репозиторий:
   ```bash
   git clone <url>
   cd brand-shop

2. Установите зависимости:
   ```bash
   npm install

3. Запустите проект в режиме разработки:
   ```bash
   npm start

  Откройте http://localhost:3000.

4. Для сборки production-версии:
   ```bash
   npm run build
   
Сергей Билямов – [ссылка на GitHub](https://github.com/SergeyBilyamov)

Проект создан в учебных целях для отработки навыков React и адаптивной вёрстки.
